# 凭据治理与 Git 历史清理待办（WP0 现场侧）

状态：**软件侧已完成（2026-08-20）**，本文件记录必须由现场/用户执行的安全处置项。对应 `SCADA只读可信化与现场试运行-SPEC-PLAN.md` 第 11.5 节与 WP0。

## 背景

以下真实凭据曾写入被 Git 跟踪的文档并推送到远端（GitHub），**当前工作树已脱敏，但完整 Git 历史中仍然存在**：

| 凭据 | 曾出现位置 | 现状 |
| --- | --- | --- |
| 无线 SSID `wushuizhan` + 密码 | `网桥测试/网桥配置记录.md`（多版历史） | 当前树已脱敏；历史仍在 |
| 无线密码 `123456789` | 同上 | 同上 |
| 网桥管理口令（出厂默认 `admin01`） | 网桥配置记录、药剂房配置记录、AGENTS.md | 当前树已脱敏；历史仍在 |
| M100 Web 口令（出厂默认 `admin`） | 工控机部署指南、M100-IP配置文档 | 当前树已脱敏；历史仍在 |

真实凭据现在只保存在本地被忽略的 `docs/integration/本地凭据.local.md`（已验证被 gitignore 覆盖、不被跟踪）。

## 软件侧已完成（本仓库）

- [x] 全部跟踪文档脱敏（2026-08-20，见上表"现状"列）。
- [x] `scripts/checks/security/check-secrets.mjs`：已知凭据字面量 + 非空凭据赋值 + local 文件跟踪检查，`npm run check:secrets` 当前 215 个跟踪文件扫描为 0 违规。
- [x] `ScadaHub.csproj` 显式排除 `appsettings.local.json` 的构建/发布复制。
- [x] `.gitignore` 排除 `.zcode/`、`releases/`、发布包目录、本地凭据文件。
- [x] `SCADA_DISABLE_ALL_DEVICE_IO` 硬门禁 + Testing 环境自动抑制 + 设备级 `Enabled`（fail-closed），并有测试证明配置启用时仍 0 次设备调用。
- [x] runtime smoke 强制 Testing + 硬门禁并断言不注册任何设备传输。

## 现场/用户必须执行（阻止发布，不阻止继续软件整改）

1. **轮换凭据**（顺序建议）：
   - 无线网桥：修改主网桥与全部从站的 SSID/密码（出厂管理口令一并修改）。
   - M100：逐台修改 `.7/.8/.31/.32` 的 Web 登录口令（逐台直连操作，改前先断 WLAN2/核对 SN）。
   - 修改后更新本地 `docs/integration/本地凭据.local.md` 与现场设备，并同步 `appsettings.local.json`。
2. **Git 历史清理**（轮换完成后执行）：
   - 备份仓库（含远端）→ 用 `git filter-repo`（或经批准的等效方案）移除历史中的凭据字符串 → 强推远端 → 所有克隆副本重新克隆或 `git pull --rebase` 后清 reflog/AGC。
   - 清理后运行**全 history 扫描**（`git log -p --all | grep -E 'wushuizhan|123456789|admin01'` 为空）并留档证据。
   - 注意：历史重写会使既有 commit SHA 全部变化，SPEC-PLAN 的基线提交号需同步更新。
3. **复核**：再次运行 `npm run check:secrets` 与全 history 扫描，结果归档到 Gate A 证据。

## 明确不做 / 未做

- 未执行历史重写（需要单独备份与远端协调，见 SPEC-PLAN 11.5）。
- 未轮换任何现场设备凭据（物理操作，需现场执行）。
- ProgramData 外部配置与 ACL（WP4/WP5 范围）。
