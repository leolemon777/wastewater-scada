# M100 IP 配置 — 现行版本

本文件是 M100 数据网段（`192.168.0.x`）IP 分配的**唯一现行权威版本**，更新于 2026-08-19（用户确认）。

- 历史版本（v1 `.7`~`.13`、v2 `.30`~`.34`）见 [`../archive/integration/M100-IP规划-历史归档.md`](../archive/integration/M100-IP规划-历史归档.md)。
- 版本规则：任何记录与本文件冲突时，以**修改日期最新**的记录为准；本文件应始终保持最新。
- 网桥管理网段 `192.168.2.x` 是另一套独立体系，不在本文件维护范围（见 `网桥测试/网桥配置记录.md`）。

## 现行 IP 分配表（2026-08-19）

| M100 IP | 现场模块 | SN | 网桥 | 状态 |
| --- | --- | --- | --- | --- |
| `192.168.0.8` | 中间池地下池液位 | `02700125040300025737` | `.68` | ✅ 2026-07-02 已改并验证（AI2=16.916mA，0-5m 量程≈4.04m） |
| `192.168.0.30` | 收集池液位 | `02700125040300029879` | `.72`（待确认） | 现行分配（2026-08-19 用户确认） |
| `192.168.0.31` | 气浮前端控制（电柜） | `02700125040300029684` | `.70` | ✅ 2026-08-19 已验证（ping 4/4，平均43ms；`ioread.cgi`: do=[1,1] 曝气/刮沫开；Web 已开启登录认证） |

> **气浮 AI1 换算（2026-08-19 用户确认）**：AI1 为 4-20mA 模拟量，对应 PH 量程 0-14，公式 `pH = (mA - 4) / 16 * 14`。当日实测 9516 uA = 9.516 mA ≈ pH 4.83。此通道为 PH 变送模拟输出，可绕开 RS485 主站冲突直读 PH（RS485 点表 `node0101/40001` 仍归有人云采集，两者数据源同仪表）。
| `192.168.0.32` | 燃气进水流量计 | 待确认 | 待确认 | 现行分配（2026-08-19 用户确认） |
| `192.168.0.33` | 药剂房电机 | 待确认 | `.78`？（待确认） | 现行分配（2026-08-19 用户确认） |
| `192.168.2.80` | 水泵电机远程操作 | 待补 SN | 交换机直连（不走网桥） | ✅ 2026-07-02 已改并验证（管理网段） |

说明：

- `.30/.31/.32/.33` 为用户 2026-08-19 确认的最新版本。建议逐台 `http://<ip>/ioread.cgi?read` 复核并回填实测值（尤其网桥待确认的三台）。
- **混合池/排水池设备变迁（2026-08-19 用户确认）**：原排水池 M100（SN `02700125040300030203`）已随 `.67` 网桥整体迁至混合池位置（`.69` 混合池网桥损坏废弃），其数据现为**混合池数据**；该设备 IP 仍未分配，接入混合池数据时需现场改 IP 并按混合池点位建表。排水池后续另配新设备（新网桥 + 新 M100，IP 届时分配）。
- 已取消纳入：混合池 PH1、收集池流量计、纯水房（纯水房已改走三菱 PLC 只读链路，见 `纯水房三菱PLC只读采集模块-SPEC.md`）。
- 气浮前端控制 = 气浮前端电柜，同一台设备，不要拆成两台。

## 改 IP 标准流程（M100 内置网页）

1. 网线直连目标 M100；电脑网卡设 `192.168.0.100` / `255.255.255.0`。
2. 浏览器 `http://192.168.0.7` → 账号 admin / 密码 admin（注意与网桥的 `admin01` 不同；密码不写入 git 文档）。
3. **先核对页面 SN 是目标设备**（多台默认 `.7`，插错就改错）。
4. 「以太网/EtherNET」→ IP 获取方式选 Static IP → 填本机 IP / 掩码 `255.255.255.0` / 网关 `192.168.0.1`。
5. 保存 → 自动重启约 30s → 用新 IP 访问 `ioread.cgi` 验证。

## 关键操作与红线

- 读 IO：`http://192.168.0.x/ioread.cgi?read`（只读本机 DO/DI/AI；读不到 RS485 下挂仪表如 PH）。
- 写 DO：`http://192.168.0.x/iowrite.cgi?do01=1` —— **未确认接线前不要盲写**。
- M100 不支持 TLS，不要用 8883 MQTT。
- 有人云边缘与本地边缘计算网关互斥，不能靠 `edgeen=1` 做双路采集（已验证会关有人云）。
- 每次只让一台待配置 M100 在线，或由现场明确隔离，避免改错设备。
- ⚠️ 电脑 WLAN 2 的 `192.168.0.10` 是连有人云/M100 网段的，**不要动**。

## 待办

- [ ] `.30/.31/.32/.33` 逐台复核 `ioread.cgi`，回填实测值与 SN（燃气流量计、药剂房电机）。
- [ ] 确认收集池液位网桥（`.72`）、燃气进水流量计网桥、药剂房电机网桥（`.78` 后当前挂的是上位机 `.79` 而非 M100，需现场核实）。
- [ ] 网桥 `.68/.69/.70/.72/.76` 连通性复测（2026-08-10 未测）。
- [ ] `.79` 药剂房上位机不通排查；`.74` 纯水房网桥 DHCP 改静态。
