# 最新版完整合并说明

日期：2026-07-23

本次将三个真实修改来源收口到当前主工程：

1. `main` 当前工作区中的 `src/components/scene` 新架构及后续修改。
2. `wanglizhou523/research-pipeline-paths` 中 `8ff71a3`、`857b7c0`、`40a5d6e` 的有效改动。
3. `stash@{0}`（整合前备份）的三文件改动。

五个 `claude/eider/hawkfish/knifefish/mackerel` 分支均指向同一个初始提交，没有五套不同代码。

## 合并原则

- 同一功能发生冲突时，以时间更晚、能够通过当前新架构检查的实现为准。
- 研究分支中的管路、泵体、柜体、仓库和物流修复按新目录移植。
- 当前工作区中更晚的统一布局、精确密封面、刚性泵组、化学计量泵布局和障碍物检查继续保留。
- stash 中的巡检人员/路线修改已被后续清理提交删除，不重新引入不可达旧模块。
- stash 中 2# 柜 `z=-4.2` 的过渡位置由更晚研究版 `[5.5, 0.5, 5.0]` 取代。

## 已收口的功能

- 三台室外控制柜采用研究版最终位置与朝向。
- 泵吸入口和排出口贴合泵壳，路由锚点同步更新；泵组保持刚性，不随运行动画脱离管线。
- 工艺泵吸水管采用“池内 0.4 m → 墙套管 → 泵密封面”的连续路径。
- 泵端变径、汇管连续壳体、污泥外送连续管和池体穿墙连接全部保留。
- 出水阀采用研究版缩放和位置。
- 进水提升泵取消自由气源，全部连接实际收集池墙面并避开池角。
- 危废仓库补齐结构柱、危险品标识、百叶、防泄漏围堰、消防/吸附设施和屋顶通风。
- 叉车卸料点深入仓库，地面高度按仓库地坪解析。
- 危废管口参考截图并入 `docs/reference/`。
- 静态几何烘焙删除无意义逐帧回调；溢流纹理随机种子按索引和盐值稳定生成。

## 自动验证

- `check-latest-merge-contracts.mjs`：柜体、泵口、阀门、仓库和叉车合并约束。
- `check-pipe-physical-connections.mjs`：泵、墙口、汇管、变径和廊道约束。
- `npm run check:scene`：27 项检查。
- `npm run verify:scene`：场景检查和生产构建。
- `npm run lint`：代码规范检查。
