/** Locked scene appearance — bright daytime, sunny, always full render quality. */
export const SCENE_VISUAL = {
  isBrightPalette: true,
  isNight: false,
  isDaylight: true,
  isSunny: true,
  isCloudy: false,
  isRainy: false,
} as const;
